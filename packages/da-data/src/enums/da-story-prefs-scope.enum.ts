/**
 * Indicates the function range of a daylightful story preference
 *
 * - `GLOBAL` for global range
 * - `STORY` for story range
 *
 * @export
 * @enum {string}
 */
export enum DaStoryPreferencesScope {
  GLOBAL="GLOBAL",
  STORY="STORY",
}